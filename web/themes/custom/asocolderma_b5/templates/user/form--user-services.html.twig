{#
/**
 * @file
 * Theme override for a 'form' element.
 *
 * Available variables
 * - attributes: A list of HTML attributes for the wrapper element.
 * - children: The child elements of the form.
 *
 * @see template_preprocess_form()
 */
#}

<div class="pre-form">
  <div class="pre-form__top">
    {{ drupal_block('text_resize_block') }}
    <div class="pre-form__status">
        <span>Estado de afiliación:</span>
        <span class="text">NO VERIFICADO</span>
        <a class="btn btn--primary" href="/usuario/estado-afiliacion">Resumen</a>
    </div>
  </div>
</div>
<div class="form-information">
  <h1>Actualizar servicios</h1>
  <p class="views">Total visitas a mi perfil:<strong>0</strong></p>
  <div class="form-information__bottom">
      <p>
          Seleccione los servicios que presta en su consulta en la siguiente lista. Recuerde que esta información será publicada en su perfil del Directorio de Dermatólogos donde los pacientas podrán visualizarla.
      </p>
      {{ user_link }}
  </div>
</div>
<form{{ attributes }}>
  {{ children }}
</form>
